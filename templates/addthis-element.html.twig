{#
/**
 * @file
 * Default theme implementation to display a node..
 * Available variables:
 * - element: AddThis Wrapper to be rendered
 * - element['']: Something else to render.
 *
 */
#}
<{{ element['#tag'] }} >
{{ element }}
</{{ element['#tag'] }}>
